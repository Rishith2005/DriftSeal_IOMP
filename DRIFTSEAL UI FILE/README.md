
  # DRIFTSEAL`

  This is a code bundle for DRIFTSEAL`. The original project is available at https://www.figma.com/design/zCC7w2apNghbXhnXzhl19a/DRIFTSEAL%60.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  